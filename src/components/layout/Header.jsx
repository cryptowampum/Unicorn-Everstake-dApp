// Application header component
import React from 'react';

const Header = ({ walletAddress, currentChain }) => {
  return (
    <header className="app-header">
      {/* TODO: Implement header with wallet info */}
      <h1>Unicorn POL Staking</h1>
    </header>
  );
};

export default Header;
